template:
  name: My template
  description: some description
  author: name <adsad@sadasd.com>
  license: cc-0

params:
  default:
    required: true
    validator: string

layout:
  template: |
    <p>{{param.default}}</p>
  css: |
    p.test { color: red; }
    .label {font-size: 0.8em;}
